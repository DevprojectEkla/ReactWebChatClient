import React from 'react'
import {StyledSpinner, StyledSpinnerContainer} from '../styles/SpinnerStyles'

const Spinner = () => 
{
    <StyledSpinnerContainer>
        <StyledSpinner/>
        </StyledSpinnerContainer>
}

export default Spinner;
